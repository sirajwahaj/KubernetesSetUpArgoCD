apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  # PostgreSQL
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_USER: "guestbook"
  DB_NAME: "guestbook"

  # Redis
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"

  # App
  PORT: "8080"
